mode: append
items:
- Sync the approved Chummer design bundle into `ui-kit` under `.codex-design/` and refresh repo-local review context.
- Add milestone mapping or executable queue work for Shared token, shell chrome, and accessibility primitives are still not a real package-only boundary consumed by presentation and play..
- Publish or append runnable backlog for Shared token, shell chrome, and accessibility primitives are still not a real package-only boundary consumed by presentation and play..
- Finish milestone coverage modeling for ui-kit so ETA and completion truth are no longer partial.
- Add milestone mapping or executable queue work for Dense data, state badges, explain chips, and Chummer-specific reusable patterns are still implicit in app repos instead of centralized here..
- Publish or append runnable backlog for Dense data, state badges, explain chips, and Chummer-specific reusable patterns are still implicit in app repos instead of centralized here..
- Add milestone mapping or executable queue work for Catalog, visual-regression, and release discipline for the shared kit are not yet materialized..
- Publish or append runnable backlog for Catalog, visual-regression, and release discipline for the shared kit are not yet materialized..
- ui-kit U7: Implement package-owned catalog coverage in `src/Chummer.Ui.Kit/Preview/PreviewGalleryManifest.cs` and add deterministic Blazor/Avalonia payload regression assertions in `tests/Chummer.Ui.Kit.Tests/Program.cs`.
- ui-kit U8: Add release-discipline gates in `README.md` plus verify-path packaging checks (`dotnet pack` + `scripts/ai/verify.sh`) with required downstream adoption evidence for presentation/play.
