
# UI Kit Implementation Scope

`chummer-ui-kit` owns design tokens, shared shell chrome, accessibility primitives, state badges, and cross-head visual building blocks.

Must not own:
- domain DTOs
- HTTP clients
- local storage logic
- rules math

Current focus:
- become the package-only shared UI boundary for presentation and play
